name: 🤖 Bob Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    name: Bob reviews this PR
    # Skip draft PRs and bot-authored PRs to avoid loops
    if: >
      github.event.pull_request.draft == false &&
      !endsWith(github.event.pull_request.user.login, '[bot]')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pull-requests: write
      contents: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for gh pr diff to resolve merge base

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Bob Shell
        run: curl -fsSL https://bob.ibm.com/download/bobshell.sh | bash -s -- --pm npm

      - name: Fetch PR diff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr diff ${{ github.event.pull_request.number }} > pr.diff

      - name: Run Bob Shell review
        env:
          BOBSHELL_API_KEY: ${{ secrets.BOBSHELL_API_KEY }}
        run: |
          bob --accept-license --auth-method api-key -p "$(cat <<'EOF'
          You are reviewing a pull request on the IBM/galaxium-travels repo, non-interactively, in CI.
          Complete all investigation before writing a single word of the review.

          ## Repo context — read this first
          Galaxium Travels is a *demo application* purpose-built to showcase IBM Bob's capabilities.
          Unlike a normal production codebase, intentional complexity is expected and acceptable here.
          PRs typically fall into one of three categories — you must identify which one applies before
          writing a single finding:

          1. **Feature / infrastructure change** — genuine additions or fixes to the app itself
             (new endpoints, UI components, CI config, Docker setup, etc.).
             Review these normally; correctness and convention compliance matter.

          2. **Demo scenario** — code whose primary purpose is to set up a Bob demonstration.
             These PRs are often deliberately convoluted: they may pre-stage branches, include
             intentional bugs for Bob to find, commit odd-looking scaffolding, or trigger workflows
             in unusual ways. Complexity or apparent oddness is NOT a finding for this category —
             instead, describe what the demo scenario appears to be.

          3. **Learning assignment** — a trivial change (renamed variable, added comment, tiny
             refactor) made by someone working through an official Bob learning exercise.
             These sometimes land on the main branch by accident. Flag this clearly at the top of
             your review ("This looks like a learning assignment PR") and keep the review brief —
             do not pile on style nits for intentionally simple changes.

          ## Step 1 — understand the repo
          Read AGENTS.md to understand this repo's conventions, architecture, and known footguns.

          ## Step 2 — read the diff
          Read pr.diff (file_path="pr.diff"). Note every file touched and every symbol that was
          added, changed, or removed.

          ## Step 3 — investigate
          For each touched file, read it in full — the diff alone lacks the context needed to reason
          about correctness. Then grep the repo for every changed symbol and read the call sites to
          verify nothing is broken. Follow the change across service boundaries: if an endpoint,
          schema, or shared type changed, check the consumers on the other side (frontend, MCP tools,
          Java service, tests). Before adding any finding to your list, re-read the relevant lines in
          the source file to confirm the issue is real. If a pattern looks suspicious but AGENTS.md
          describes it as intentional, it is not a bug. If you are uncertain, read more — do not
          write a hedged finding.

          ## Step 4 — write the review
          Only after finishing Step 3, output the review to stdout.
          Do NOT call write_to_file or any file-writing tool. Bracket the review with these two
          marker lines exactly, each on its own line:

          <<<BOB_REVIEW_BEGIN>>>
          ...your review markdown...
          <<<BOB_REVIEW_END>>>

          The review must contain:
          - A 1-2 sentence summary at the top, followed by **PR category: Feature / Demo scenario /
            Learning assignment** (pick the best fit and add one sentence explaining why).
          - For **demo scenario** PRs: a short paragraph describing what the scenario appears to
            be demonstrating, then any genuine blockers only (skip nits entirely).
          - For **learning assignment** PRs: a brief, encouraging note. No findings section unless
            there is an outright blocker.
          - For **feature / infrastructure** PRs: a "Findings" section grouped by severity:
            blocker / nit / question. Each finding must include:
              * The exact file path and line number you read (not guessed from the diff).
              * A concrete, specific description of the problem.
              * A short, actionable suggestion.
          - If no real issues were found, say so plainly. Do not invent findings.
          - Do not restate what the diff already shows.
          - Do not self-correct or retract findings mid-review — if you are not certain, do not
            raise it. Uncertainty means you need to read more code, not write a hedged finding.
          - Do not approve or block the PR — you are leaving a comment, not a verdict.

          Do not modify any source files. Do not run the test suite. Do not write any files.
          EOF
          )" 2>&1 | tee bob_output.txt

      - name: Extract review from Bob's stdout
        run: |
          python3 -c "
          import re, sys
          text = open('bob_output.txt').read()
          m = re.search(r'<<<BOB_REVIEW_BEGIN>>>(.*?)<<<BOB_REVIEW_END>>>', text, re.DOTALL)
          if m:
              print(m.group(1).strip())
          else:
              print('_Bob did not produce a structured review. Raw output below._\n')
              print(text)
          " > review_body.md
          {
            echo "## 🤖 Bob's Review"
            echo ""
            echo "*Posted by [Bob Shell](https://bob.ibm.com), IBM's CLI agent — running headless on GitHub Actions.*"
            echo ""
            echo "---"
            echo ""
            cat review_body.md
          } > review.md
          echo "--- Final review ---"
          cat review.md

      - name: Post review comment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr review ${{ github.event.pull_request.number }} --comment --body-file review.md